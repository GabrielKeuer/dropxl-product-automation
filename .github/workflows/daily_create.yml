name: Daglig Produkt Oprettelse

on:
  schedule:
    - cron: '20 3 * * *'   # 05:20 København (sommer) — v2 cutover 2026-06-03
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-toggle:
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ steps.check.outputs.enabled }}
    steps:
      - name: Check automation toggle
        id: check
        run: |
          ENABLED="${{ vars.AUTOMATION_ENABLED || 'true' }}"
          echo "enabled=$ENABLED" >> $GITHUB_OUTPUT
          if [ "$ENABLED" != "true" ]; then
            echo "⚠️ Automation er slået fra (AUTOMATION_ENABLED=$ENABLED)"
          fi

  create:
    needs: check-toggle
    if: needs.check-toggle.outputs.enabled == 'true'
    runs-on: ubuntu-latest
    outputs:
      new_count: ${{ steps.run.outputs.new_count }}
      merge_count: ${{ steps.run.outputs.merge_count }}
      variant_count: ${{ steps.run.outputs.variant_count }}
      skipped_count: ${{ steps.run.outputs.skipped_count }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas requests openpyxl beautifulsoup4 supabase
      - name: Kør hurtig produkt oprettelse (v2 direct API)
        id: run
        env:
          FEED_URL: ${{ vars.FEED_URL }}
          SHOPIFY_STORE: ${{ vars.SHOPIFY_STORE }}
          SHOPIFY_ACCESS_TOKEN: ${{ secrets.SHOPIFY_ACCESS_TOKEN }}
          MAX_PRODUCTS_PER_RUN: ${{ vars.MAX_PRODUCTS_PER_RUN || '999' }}
          MAX_VARIANTS_PER_RUN: ${{ vars.MAX_VARIANTS_PER_RUN || '999' }}
          PRODUCT_ORDER: ${{ vars.PRODUCT_ORDER || 'newest' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/create_products_v2.py --live

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Tilføj kun output-filer der faktisk findes. Skip-filerne oprettes
          # kun når der er noget at skippe; git add på en manglende fil giver
          # exit 128 og dræbte tidligere commit+push selvom produkter var oprettet.
          for f in output/product_specs_v2.json output/skipped_large_products.json output/skipped_options_to_add.json output/daily_variant_count.txt; do
            if [ -f "$f" ]; then git add "$f"; fi
          done
          git diff --staged --quiet && echo "Ingen ændringer" && exit 0
          git commit -m "✨ Hurtig v2: ${{ steps.run.outputs.new_count }} nye + ${{ steps.run.outputs.merge_count }} merge, ${{ steps.run.outputs.variant_count }} var, ${{ steps.run.outputs.skipped_count }} skipped ($(date +%Y-%m-%d))"
          git pull --rebase
          git push

  notify:
    needs: [check-toggle, create]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Summary
        run: |
          echo "## ✨ Daglig Oprettelse (hurtig)" >> $GITHUB_STEP_SUMMARY
          if [ "${{ needs.check-toggle.outputs.enabled }}" != "true" ]; then
            echo "⏸️ **Automation slået fra**" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Metric | Værdi |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Nye produkter | ${{ needs.create.outputs.new_count }} |" >> $GITHUB_STEP_SUMMARY
            echo "| Merge | ${{ needs.create.outputs.merge_count }} |" >> $GITHUB_STEP_SUMMARY
            echo "| Varianter | ${{ needs.create.outputs.variant_count }} |" >> $GITHUB_STEP_SUMMARY
            echo "| Skipped (store) | ${{ needs.create.outputs.skipped_count }} |" >> $GITHUB_STEP_SUMMARY
          fi
